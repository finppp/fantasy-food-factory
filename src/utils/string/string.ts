export const capitaliseFirstChar = (string: string) => {
  return string.charAt(0).toUpperCase() + string.slice(1)
}

export const lowerCaseFirstChar = (string: string) => {
  return string.charAt(0).toLowerCase() + string.slice(1)
}
